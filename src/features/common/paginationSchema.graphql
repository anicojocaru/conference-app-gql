  input PagerInput {
  page: Int!
  pageSize: Int!
}

type Page {
  page: Int!
  pageSize: Int!
}

type Pagination {
  currentPage: Page
  totalCount: Int
}
  
  
  
  
  # input PagerInput {
  #   afterId: ID
  #   sortBy: String
  #   direction: Int
  #   pageSize: Int
  # }

  # type Page {
  #   afterId: ID
  #   sortBy: String
  #   direction: Int
  #   pageSize: Int
  # }

  # type Pagination {
  #   totalCount: Int
  #   prevPage: Page
  #   nextPage: Page
  # }